'use strict';

describe('myApp.list_student module', function() {

  beforeEach(module('myApp.list_student'));

  describe('list_student controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listStudentCtrl = $controller('ListStudentCtrl');
      expect(listStudentCtrl).toBeDefined();
    }));

  });
});